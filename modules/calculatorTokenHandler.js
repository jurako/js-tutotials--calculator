/**
 * Parses input characters into tokens
 * and stores all tokens in array
 */
export class TokenHandler {
    constructor() {
        this.tokens = [];
        this.currentToken = 0;
    }

    parse(input) {

        switch (input) {
            case value:

                break;

            default:
                break;
        }

    }

    store() {

    }

    clear() {

    }
}